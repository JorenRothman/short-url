import LoginForm from "@/components/auth/loginForm";

export default function Register() {
    return (
        <main className="container mx-auto flex h-full w-full items-center">
            <LoginForm />
        </main>
    );
}
